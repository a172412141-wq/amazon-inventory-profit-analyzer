# PLANS.md

本计划描述中长期实施顺序。具体当前状态以 `HANDOFF.md` 为准。

## 总体目标

把项目建设成一个可审计、可配置、可重复验证的本地 Amazon 经营分析工具：从 Excel 输入形成 SKU 动作、角色分析、父体/SPU/品线诊断和可执行行动闭环，同时明确数据可信度与结论边界。

## P0：恢复可运行基线（已完成，2026-06-18）

目标：消除当前启动阻断，保护既有动作逻辑。

- 恢复 `modules/recommendations.py`。
- 完成导入、全量测试、Streamlit 启动和 Excel 导出冒烟测试。
- 清理或忽略 `.DS_Store`，避免污染提交范围。

验收结果：应用可启动；43 个测试通过；现有 Fang 改动和三级分类联动筛选未回归。

## P1：完成 Fang 品线诊断闭环

目标：让“数据可信度 → 关系诊断 → 原因等级 → SKU/品线 ToDo”形成可信、可执行的链路。

- 可信度计算读取原始缺失标记，而不是把清洗后的 0 当作真实来源值。
- 明确“已确认原因 / 高概率原因 / 待验证假设 / 无法判断”的判定边界。
- 保证报告层不覆盖系统动作和优先级。
- 决定并实现 ToDo 编辑结果的会话保存、导出或其他持久化方式。
- 增加边界场景测试：全缺失、零销量、有库存无销量、单 SKU 品线、缺父体、缺尺寸、混合时间窗口。

验收：缺失数据不会被错误标记为完整；ToDo 有明确保存/导出语义；新增测试通过。

## P1：端到端输入与导出验证

目标：验证真实 Excel 从上传到报告输出的完整流程。

- 准备去敏的小型输入样例和预期结果说明。
- 验证多 Sheet、非首行表头、中英文别名、百分比三种格式。
- 验证 11 个现有导出 Sheet；若 ToDo 进入导出，补充独立 Sheet 和格式检查。
- 对关键指标做人工复算：可售库存天数、ACOS、ACOAS、父体毛利率、角色分类。

验收：样例可重复生成一致结果，关键公式人工复算一致，导出文件可正常打开。

## P2：规则可审计与可配置

目标：减少散落在代码中的阈值和隐式口径。

- 盘点代码中的硬编码阈值，判断哪些应进入 `thresholds.yaml`。
- 为动作决策和品线诊断输出规则版本或配置快照。
- 建立业务规则与测试用例的对应关系。
- 补充异常数据降级策略和结论置信度说明。

验收：核心判断可追溯到配置或明确记录的固定规则；改阈值有覆盖测试。

## P2：工程与交付稳定性

目标：让本地运行、测试和发布方式可重复。

- 固化标准测试入口和 Streamlit 冒烟流程。
- 增加 CI，至少执行导入检查、pytest 和 diff/格式检查。
- 明确 Python 版本和依赖锁定策略。
- 解决外层/内层双 Git 仓库的长期归属问题，避免误提交。

验收：新环境按 README 可复现；CI 能阻止启动失败和核心规则回归。
